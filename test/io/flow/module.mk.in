DIR := test/io/flow

TEST_IO_FLOW_DIR_SRC := \
	$(DIR)/test-harness-server-trove.c \
	$(DIR)/test-harness-client.c 	\
	$(DIR)/test-bmi-cache-server.c  \
	$(DIR)/test-bmi-cache-client.c  \
	$(DIR)/test1-client.c  \
	$(DIR)/test1-server.c

TESTSRC += $(TEST_IO_FLOW_DIR_SRC)

test/io/flow/test-harness-server-trove: test/io/flow/test-harness-server-trove.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
endif

test/io/flow/test-harness-client: test/io/flow/test-harness-client.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(LIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(LIBS) -o $@
endif

test/io/flow/test-bmi-cache-server: test/io/flow/test-bmi-cache-server.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
endif

test/io/flow/test-bmi-cache-client: test/io/flow/test-bmi-cache-client.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(LIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(LIBS) -o $@
endif

test/io/flow/test1-server: test/io/flow/test1-server.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
endif

test/io/flow/test1-client: test/io/flow/test1-client.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(LIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(LIBS) -o $@
endif
