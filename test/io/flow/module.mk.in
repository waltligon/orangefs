DIR := io/flow

TEST_IO_FLOW_DIR_SRC := \
	$(DIR)/test-harness-server-trove.c \
	$(DIR)/test-harness-client.c 	\
	$(DIR)/test-bmi-cache-server.c  \
	$(DIR)/test-bmi-cache-client.c  \
	$(DIR)/test1-client.c  \
	$(DIR)/test1-server.c

TESTSRC += $(TEST_IO_FLOW_DIR_SRC)

io/flow/test-harness-server-trove: io/flow/test-harness-server-trove.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
endif

io/flow/test-harness-client: io/flow/test-harness-client.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(LIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(LIBS) -o $@
endif

io/flow/test-bmi-cache-server: io/flow/test-bmi-cache-server.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
endif

io/flow/test-bmi-cache-client: io/flow/test-bmi-cache-client.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(LIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(LIBS) -o $@
endif

io/flow/test1-server: io/flow/test1-server.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(SERVERLIBS) -o $@
endif

io/flow/test1-client: io/flow/test1-client.o
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $< $(LDFLAGS) $(LIBS) -o $@
else
	$(LD) $< $(LDFLAGS) $(LIBS) -o $@
endif
