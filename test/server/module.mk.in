DIR := server

TESTSRC += \
	$(DIR)/showconfig.c

test/server/showconfig: test/server/showconfig.o lib/libpvfs2-server.a
ifdef QUIET_COMPILE
	@echo "  LD		$@"
	@$(LD) $^ $(LDFLAGS) $(SERVERLIBS) -o $@
else
	$(LD) $^ $(LDFLAGS) $(SERVERLIBS) -o $@
endif
